import React from 'react';
import ContactsList from './ContactsList';

const Home = () => {
    return (
        <section>
            <ContactsList />
        </section>
    );
}

export default Home;